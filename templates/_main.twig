{% extends '_base' %}

{% if entry.section.handle|default(null) == 'moduleListing' and not currentUser %}
    {% exit 404 %}
{% endif %}

{% block navigation %}
    {{ include('common/_topBar') }}
    {{ include('common/_navigation') }}
{% endblock %}

{% block footer %}
    {{ include('common/_footer') }}
{% endblock %}
