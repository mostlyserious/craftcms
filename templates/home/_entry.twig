{% extends '_main' %}

{% block main %}
    {{ include('headers/_entry', {
        block: entry.header.one,
    }) }}

    {{ include('blocks/_entry', {
        nested: false,
        branded: true,
    }) }}
{% endblock %}
